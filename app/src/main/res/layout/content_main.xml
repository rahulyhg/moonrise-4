<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <FrameLayout
        android:id="@+id/zero_layout"
        android:layout_width="@dimen/zero_outer_size"
        android:layout_height="@dimen/zero_outer_size"
        android:layout_centerInParent="true">

        <View
            android:id="@+id/zero_ocean"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="@drawable/zero_ocean" />

        <View
            android:id="@+id/zero_panorama"
            android:background="@drawable/moonrise_panorama"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:id="@+id/zero_moon"
            android:layout_width="@dimen/zero_sun_outer_size"
            android:layout_height="@dimen/zero_sun_outer_size"
            android:layout_gravity="center" />

    </FrameLayout>


</RelativeLayout>
